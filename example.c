#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void funcao() {
int inteiro = 0;
for(inteiro; inteiro < 10; inteiro++) {
printf("Numero %d", inteiro);
}
}

int main() {
funcao();
    return 0;
}
